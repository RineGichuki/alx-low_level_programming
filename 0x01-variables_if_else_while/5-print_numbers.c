#include <stdio.h>
#include <string.>
#include <ctype.h>

/* more headers go there */

/**
 * main - Entry point
 i*    * Return: Always 0 (Success)
 */
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
	{
		printf("%d", number);
	}
	putchar('\n');
	return (0);
}
